node_modules/

# `**/` matters: an agent's working directory can be at any depth. Agent memory belongs to the
# agent's own config repository, and an ephemeral worktree is a nested repository rather than content.
**/.claude/agent-memory/
**/.claude/worktrees/
